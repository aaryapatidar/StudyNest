# System Requirements

Users can register, sign in, create subjects, manage notes, manage tasks, save resources, track revision and view progress. The system should be responsive, accessible, validated, secure by default, documented and deployable with Docker.
